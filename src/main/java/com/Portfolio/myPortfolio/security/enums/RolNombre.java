package com.Portfolio.myPortfolio.security.enums;

public enum RolNombre {
    //El aplicativo tiene dos roles:
    // admin(Tiene todos los permisos CRUD) y user(solo read)
    ROLE_ADMIN, ROLE_USER
}